<% unless @item.errors.any? %>
	var th = $('<%= escape_javascript(render @item) %>').css('opacity', 0);
	_.$('items:parent').prepend(th);
	th.animate({opacity: 1}, {duration: 1000, queue: false})
<% else %>
	alert('error.');
<% end %>