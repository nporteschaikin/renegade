%article
	= simple_format comment.comment
	%aside
		= raw "#{link_to comment.user.username, comment.user} posted this #{link_to time_ago_in_words(comment.created_at), comment} ago"
- if comment.comments.any?
	%section
		= render comment.comments